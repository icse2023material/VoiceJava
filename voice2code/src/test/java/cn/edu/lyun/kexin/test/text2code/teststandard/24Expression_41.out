public class HelloWorld {

    public void sayHello() {
        if (obj instanceof ClusterInvoker) {
            boolean isClusterInvoker = obj instanceof ClusterInvoker;
            isInstance = obj instanceof ClusterInvoker;
        }
    }
}
