public class HelloTest {

    public void main(String[] args) {
        int[] ns;
        for (int i = 0; i < ns.length; i++) {
            System.Out.println(ns[i]);
            int nsi = ns[i];
            nsi = ns[i];
        }
        return ns[i];
    }
}
